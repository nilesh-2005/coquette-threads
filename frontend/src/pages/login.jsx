import Account from './account';

// Requirement: Add login page: /pages/login.jsx
// We can reuse the Account component since it already handles login/register
export default function Login() {
    return <Account />;
}
